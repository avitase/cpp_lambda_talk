\begin{lstlisting}[title=\href{https://godbolt.org/z/p_I8hF}{\texttt{godbolt.org/z/p\_I8hF}}]
#include <vector>

std::vector<int> get_ints();

int main() {
    auto ints = get_ints();
    const auto sum = [&ints] {
        int acc = 0;
        for (auto& x: ints) acc += x;
        return acc;
    }();

    return sum;
}
\end{lstlisting}
